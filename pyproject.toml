[project]
name = "hermes-portal"
version = "0.1.0"
description = "Web portal that controls a remote CDP browser over Tailscale"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "httpx>=0.27",
    "websockets>=13",
    "itsdangerous>=2.2",
    "python-multipart>=0.0.12",
    "jinja2>=3.1",
]
